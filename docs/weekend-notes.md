Questions to Answer in Your Notes:

What does RAG stand for?
    - Retrieval and augumented generation
    - concepts
        - Indexing - a pipeline for ingesting data from source and indexing it
        - retrieval and augumentation - the actual RAG process which takes user input during run time , retrives relevant data and passes it on to the model
What problem does it solve?
    - IT helps add context or additional knowledge base to user prompts to make the response aligned with the needs of the user
Why not just ask AI directly?
    - The user can ask AI directly, however the prompt becomes longer
What's the difference between RAG and fine-tuning?